{
  "name": "genlayer-supplychaingate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "python -m pytest -v",
    "lint:contract": "genvm-lint check contracts/supplychaingate.py"
  }
}
